import { describe,expect,it } from "vitest";import type { DraftPage } from "./editor-types";import { movePage,nextVersion,rotatePage } from "./editor-utils";import { draftSchema } from "./validation";
describe("page operations",()=>{it("rotates four times",()=>{let page:DraftPage={id:"p",sourceAssetId:"a",sourcePageIndex:0,rotation:0,width:100,height:200};for(let i=0;i<4;i++)page=rotatePage(page);expect(page.rotation).toBe(0)});it("moves without losing pages",()=>expect(movePage(["a","b","c"],2,0)).toEqual(["c","a","b"]));});
describe("draft version",()=>{it("increments matching version",()=>expect(nextVersion(4,4)).toBe(5));it("rejects stale version",()=>expect(nextVersion(5,4)).toBeNull())});
describe("annotation validation",()=>{const base={schemaVersion:1 as const,pages:[],objects:[]};it("allows lines drawn in every direction",()=>expect(draftSchema.safeParse({...base,objects:[{id:"line",pageId:"page",type:"line",x:100,y:100,width:-40,height:30,color:"#315ee7",opacity:1}]}).success).toBe(true));it("keeps rectangles constrained to a positive size",()=>expect(draftSchema.safeParse({...base,objects:[{id:"rect",pageId:"page",type:"rect",x:100,y:100,width:-40,height:30,color:"#315ee7",opacity:1}]}).success).toBe(false));it("keeps freehand pen points in the draft",()=>expect(draftSchema.safeParse({...base,objects:[{id:"pen",pageId:"page",type:"pen",x:10,y:10,width:20,height:15,color:"#315ee7",opacity:1,strokeWidth:3,points:[{x:10,y:10},{x:30,y:25}]}]}).success).toBe(true));});
