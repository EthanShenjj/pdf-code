"use client";
import { useState } from "react";import { useRouter } from "next/navigation";import Link from "next/link";import { Brand } from "@/components/brand";import { authClient } from "@/lib/auth-client";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const {error}=await authClient.signIn.email({email,password});if(error){setError("账号或密码不正确，请联系管理员创建内部账号");setBusy(false);}else router.push("/documents");}return <main className="grid min-h-screen md:grid-cols-[1fr_1.05fr]"><section className="flex flex-col p-7 md:p-12"><Brand/><div className="m-auto w-full max-w-sm py-12"><h1 className="text-4xl font-semibold tracking-[-.04em]">欢迎回来</h1><p className="mt-3 text-[#667085]">打开你保存的文档，继续上次的工作。</p><form onSubmit={submit} className="mt-9 space-y-5"><label className="block text-sm font-medium">邮箱<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="name@company.com" className="focus-ring mt-2 h-12 w-full rounded-xl border border-[#d8deea] px-4 outline-none"/></label><label className="block text-sm font-medium">密码<input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="至少 8 位" className="focus-ring mt-2 h-12 w-full rounded-xl border border-[#d8deea] px-4 outline-none"/></label>{error&&<p className="text-sm text-red-600">{error}</p>}<button disabled={busy} className="focus-ring h-12 w-full rounded-xl bg-[#315ee7] font-semibold text-white hover:bg-[#2346bb]">{busy?"正在登录…":"登录"}</button></form><Link href="/" className="mt-5 block text-center text-sm text-[#315ee7] hover:underline">暂不登录，在本地使用</Link></div></section><aside className="hidden overflow-hidden bg-[#17223b] p-12 text-white md:flex md:flex-col md:justify-between"><p className="max-w-sm text-3xl font-medium leading-snug tracking-[-.025em]">“文档本该让工作更轻，而不是成为工作本身。”</p><div className="relative mx-auto aspect-[4/5] w-3/5 rotate-3 rounded bg-white p-8 text-[#17223b] shadow-2xl"><div className="h-3 w-1/3 rounded bg-[#315ee7]"/><div className="mt-8 space-y-3">{[85,70,92,55,78].map(n=><div key={n} className="h-2 rounded bg-[#e5e9f0]" style={{width:`${n}%`}}/>)}</div><div className="absolute -left-16 bottom-20 rounded-xl bg-[#f16f5c] px-5 py-4 text-sm font-semibold text-white shadow-xl">签名完成 ✓</div></div><p className="text-sm text-[#aab4ca]">Paperkit 内部验证版</p></aside></main>}

