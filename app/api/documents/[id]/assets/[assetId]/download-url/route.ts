import { GetObjectCommand } from "@aws-sdk/client-s3";import { getSignedUrl } from "@aws-sdk/s3-request-presigner";import { NextResponse } from "next/server";import { prisma } from "@/lib/prisma";import { requireUser } from "@/lib/session";import { bucket,s3 } from "@/lib/s3";
export async function GET(_:Request,{params}:{params:Promise<{id:string,assetId:string}>}){const user=await requireUser();if(!user)return NextResponse.json({error:"未登录"},{status:401});const {id,assetId}=await params;const asset=await prisma.asset.findFirst({where:{id:assetId,documentId:id,status:"READY",document:{ownerId:user.id}}});if(!asset)return NextResponse.json({error:"文件不存在"},{status:404});const downloadUrl=await getSignedUrl(s3,new GetObjectCommand({Bucket:bucket,Key:asset.storageKey}),{expiresIn:600});return NextResponse.json({downloadUrl,expiresIn:600});}
export const runtime="nodejs";
